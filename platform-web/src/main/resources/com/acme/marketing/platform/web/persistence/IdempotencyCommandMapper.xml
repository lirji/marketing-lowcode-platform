<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.acme.marketing.platform.web.persistence.IdempotencyCommandMapper">

    <insert id="insertIgnore">
        INSERT IGNORE INTO mk_api_command (
            tenant_id, operation_name, idempotency_key, payload_hash, state_name,
            response_json, created_at, updated_at, expires_at
        ) VALUES (
            #{tenantId}, #{operationName}, #{idempotencyKey}, #{payloadHash}, #{stateName},
            #{responseJson}, #{createdAt}, #{updatedAt}, #{expiresAt}
        )
    </insert>

    <select id="selectForUpdate"
            resultType="com.acme.marketing.platform.web.persistence.IdempotencyCommandMapper$CommandRow">
        SELECT
            payload_hash AS payloadHash,
            state_name AS stateName,
            response_json AS responseJson,
            expires_at AS expiresAt
        FROM mk_api_command
        WHERE tenant_id = #{tenantId}
          AND operation_name = #{operationName}
          AND idempotency_key = #{idempotencyKey}
        FOR UPDATE
    </select>

    <delete id="delete">
        DELETE FROM mk_api_command
        WHERE tenant_id = #{tenantId}
          AND operation_name = #{operationName}
          AND idempotency_key = #{idempotencyKey}
    </delete>

    <insert id="insert">
        INSERT INTO mk_api_command (
            tenant_id, operation_name, idempotency_key, payload_hash, state_name,
            response_json, created_at, updated_at, expires_at
        ) VALUES (
            #{tenantId}, #{operationName}, #{idempotencyKey}, #{payloadHash}, #{stateName},
            #{responseJson}, #{createdAt}, #{updatedAt}, #{expiresAt}
        )
    </insert>

    <update id="complete">
        UPDATE mk_api_command
        SET state_name = 'COMPLETED',
            response_json = #{responseJson},
            updated_at = #{updatedAt}
        WHERE tenant_id = #{tenantId}
          AND operation_name = #{operationName}
          AND idempotency_key = #{idempotencyKey}
          AND state_name = 'PROCESSING'
    </update>

</mapper>
