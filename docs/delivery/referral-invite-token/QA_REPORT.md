# QA Report

## 环境及结果

独立临时源码快照，专用Testcontainers MySQL 8.4.11；不接受外部共享URL。初轮15项全部pass、退出0，日志build-focused.log与evidence/initial-15。新摘要超时修复仅1项复验已通过、退出0，未重跑初轮15项或整个平台。

| 场景 | 结果 |
| --- | --- |
| 256位SecureRandom token、SHA256身份、密文无原文、原版本历史许可 | 通过 |
| 12线程同key返回相同token，仅1身份/回执/发行审计 | 通过 |
| 同key改participant冲突，跨主体/组织/租户拒绝 | 通过 |
| 回显窗口到期不调用解密，同key不替换身份；显式新key生成新token | 通过 |
| token过期/撤销不能解析，身份与密文回执仍保留 | 通过 |
| 发行已提交而KMS解密失败，同key恢复原结果且不再加密候选 | 通过 |
| token+回执+审计写后失败整体回滚 | 通过 |
| 慢加密跨历史许可截止、慢解密跨回显截止均拒绝 | 通过 |
| 真实participant行锁等待跨许可截止，回滚新token/回执 | 通过 |
| 历史许可、保护或摘要来源缺失拒绝 | 通过 |
| 摘要源延迟后重新检查token过期；跨租户解析拒绝 | 通过 |
| V2两表及每字段中文注释；V1兼容 | 通过 |
| 测试AES-GCM跨tenant/请求/到期AAD替换及密文篡改拒绝 | 通过 |
| 默认保护Port不退回明文 | 通过 |
| 最后一次数据库读取越过摘要水位不能返回陈旧摘要 | 通过 |

AEAD是仅位于test-source的真实密码学替身，测试key独立于主体HMAC fixture；不代表KMS/HSM/JWKS/密钥轮换生产验收。测试对revoked_at的更新仅用于构造已撤销状态，不包含生产撤销入口。没有数据清理或24h物理删除证明。

最终证据：evidence/summary-expiry-1仅收集本次新增用例报告；evidence/source-digests.json核对29个源码/配置文件与最终被测快照一致。两次构建句柄均退出0，无运行任务。单测容器启动耗时较高，后续无DB逻辑优先纯应用测试，真实DDL/并发/回滚仍保留隔离MySQL。
