# 本轮QA

2026-09-08。环境：本机Java21、Maven离线依赖、Testcontainers MySQL8.4.11临时库；未连接共享业务库。测试里的身份、KMS、SKU、风险和履约证明为test源码替身，只证明内部算法和事务行为。

## 最终结果

| 命令/范围 | 结果 |
|---|---|
| referral-service及依赖 test | 退出0；服务196项，零失败/错误/跳过，其中数据库联合57项 |
| marketing-control-service及依赖 test | 退出0；控制面16项，含HTTP/MySQL类型及历史幂等兼容 |
| 架构、机器合同、edge-gateway专项 | 退出0；referral已实际纳入架构扫描，合同5项及网关启动/限流专项通过 |
| git diff --check | 通过 |

完整命令、日志、测试XML、基线commit与改动源码SHA256见[evidence/20260908-final/result.json](evidence/20260908-final/result.json)。ArchUnit与JUnit共用类名导致XML最终只保留2个JUnit方法；同一次成功命令还执行7个架构规则，不将单XML当作全部架构报告。各模块数字对应不同运行，不能相加为整个reactor全量或161项完整验收。

## 覆盖

资格/fanout分页与水位、观察期原始时刻、注册/首单、双边及阶梯永久身份、退款撤销与重达不重发；个人跨桶限额、最后额度竞争、调拨守恒、取消前后预占释放边界；授权并发/恢复/取消交错/过期回滚；成功与追回乱序、永久摘要、同事件/版本冲突、UNKNOWN保留、失败释放；scope普通/空/通配/跨租户隔离，汇总不重复放大与无伪造事件水位；复评幂等/并发/预算/权限/审计及事务回滚。

## 缺陷复验

SQL保留字、终态被后续资格覆盖、旧版本终态矛盾、通配权限比较、历史幂等兼容均有回归。开发时YAML缩进、迁移字段语句及Mockito委托在设桩时误调用真实Mapper的问题已修复，最终以以上退出0命令为准。失败轮次不计为通过证据。

## 未验证

真实渠道签名和取消栅栏、跨系统完整端到端、C端与Cursor真实界面、生产容量/故障、V7/V9既有存量升级演练、共享库迁移及远程CI未完成。网关测试验证配置可启动和既有限流，不等于真实OIDC全链路路由联调。结论：本轮内部回归通过，整体裂变验收未通过。
