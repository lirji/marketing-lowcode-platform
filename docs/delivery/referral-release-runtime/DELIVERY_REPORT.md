# 裂变发布本轮交付

2026-09-09。已完成 R1 可信制品验证和 R2 的 VERIFIED 耐久安装。整个发布计划仍未完成，不能开放真实参加或发奖。

## 覆盖情况

| 验收 | 本轮状态 | 证据/限制 |
| --- | --- | --- |
| AC-01 | 完成 | 双签名及严格计划校验，42 项专项 |
| AC-02 | 部分完成 | VERIFIED 耐久/并发/冲突/回滚通过；尚无完整预热 READY |
| AC-03 | 部分完成 | 结构/签名拒绝通过；权威审批/冻结 SKU 闭包待落实 |
| AC-04 | 未完成 | 未签发/消费 READY ACK，control 副本容量门禁未接线 |
| AC-05 | 未完成 | 尚无激活/回滚序号持久逻辑 |
| AC-06 | 部分完成 | 默认无许可已验证；激活与熔断水位许可未实现 |
| AC-07 | 部分完成 | 重建安装服务、数据库原文重验通过；完整运行时重启与奖励状态回归不在此切片 |
| AC-08 | 部分完成 | 默认参与仍拒绝；control 原拒绝未改；未宣称所有旧 runtime 回归完成 |

## 修改及测试

新增 ReferralReleaseVerifier、内部安装服务与仓储/Mapper/XML、V12 制品表及两套测试，增加 SPI 的 contracts/Jackson 依赖。没有新 HTTP、自动启用 Bean 或生产密钥。配置和共享服务未变。

本地完整相关回归：SPI 72、referral 服务 206；架构命令成功，JUnit 2 项；全部零失败/错误/跳过。自审发现的范围授权缺口已修复；无独立审查声明。现有 CI clean verify 已覆盖新增测试发现机制，未触发远程 Actions。

## 与计划的差异及阻塞

原 R2 希望推进完整预热与 ACK，但既有设计明确冻结 SKU 为 opaque 引用，目录真实映射尚未签收。已有 ACTIVE SKU 查询只提供 long version，不能将旧 gate 当成冻结引用证明。因此先持久保存 VERIFIED，未创建 READY 或未实现逻辑的占位 ACK。

后续需要：

1. 明确每个冻结 benefit/SKU opaque 引用到权威 benefitId/version、skuId/version 的映射及可验证闭包来源。
2. 落实 campaignId 到已审批定义/组织/店铺的可信映射，以及熔断健康水位/重放协议。
3. 完成 R2 READY/ACK → R3 激活/许可 → R4 control；默认拒绝只有在对应测试和依赖全部满足后调整。

以上业务/接口信息已请求用户确认。未推送、未合并 main、未部署、未执行共享库迁移。恢复与回滚见 RUNBOOK.md，保留持久审计事实。
