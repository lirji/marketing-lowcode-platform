# R3 本地机制 QA

2026-09-09；Java 21，离线 Maven，隔离 MySQL 8.4.11。真实临时 Ed25519 密钥；READY 使用明确测试夹具，无生产/共享数据操作。

## 验收矩阵

| 验收 | 测试范围 | 当前结果 |
| --- | --- | --- |
| R3-A | 真实签名、tenant/slot/manifest ID/签名/代次/canary/时间/未知 key/篡改 | 指令专项 21 项通过 |
| R3-B | 单调游标与审计、同内容幂等、异内容冲突、并发高低序号、同序号熔断竞争、注释迁移 | 最终 20 项 MySQL 专项全部通过 |
| R3-C | 先激活代次 2，再用更高序号回滚到已保存代次 1，旧序号拒绝 | 专项通过 |
| R3-D | 熔断持续拒绝，缺状态/缺 READY 拒绝；clear 重放不续期、有效期取最短 | 专项通过 |
| R3-E | VERIFIED 不能激活、READY 绑定错/过期/超长拒绝；本地有效仍无参与许可 | 专项通过 |
| 恢复 | 独立 JVM 从隔离库恢复序号 9、历史代次 1，重验公钥/清单/制品/激活签名 | 专项通过，输出 RESTORED 9 1 |
| 故障 | 推进后抛异常整体回滚、锁等待期间证明过期不留 cursor/audit | 专项通过 |
| 防御增强 | 持久签名损坏、存储与验签器 namespace 错配拒绝 | 最终回归通过 |

恢复探针是新的 JVM，不继承父进程内存；它验证已提交状态可独立恢复。不是常驻消费者崩溃、全套生产服务启动或跨数据中心故障演练。

## 命令与结果

- `/tmp/referral-r3-unit.log`：21 个指令专项通过。
- `/tmp/referral-r3-mysql.log`：首轮 16 项通过。
- `/tmp/referral-r3-mysql-final.log`：加入独立 JVM 和坏签名写入拒绝后 18 项通过。
- `/tmp/referral-r3-verify.log`：最终增加持久损坏和配置错配后全模块 verify，成功；SPI 93、服务 226 项均无失败/错误/跳过。

READY Port 为 unavailable 时不会激活，DEFAULT 参与许可仍返回 null。真实签发、健康订阅、活动映射、远程 Actions 及部署未执行。

最终本地切片 QA：pass。架构命令通过，JUnit 报告 2 项，零失败/错误/跳过；远程/外部验收保持未执行。
