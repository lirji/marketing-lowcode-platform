<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.acme.marketing.benefit.infrastructure.persistence.mapper.ReferralHeldReviewMapper">
<insert id="reserve">INSERT INTO mk_referral_award_held_review (tenant_id,source_system,source_request_id,binding_digest,checked,checked_nanos) VALUES (#{tenant},'marketing-referral',#{request},#{digest},#{checked},#{checked_nanos}) ON DUPLICATE KEY UPDATE source_request_id=source_request_id</insert>
<select id="lock" resultType="map" useCache="false" flushCache="true">SELECT * FROM mk_referral_award_held_review WHERE tenant_id=#{tenant} AND source_system='marketing-referral' AND source_request_id=#{request} FOR UPDATE</select>
<select id="find" resultType="map" useCache="false" flushCache="true">SELECT * FROM mk_referral_award_held_review WHERE tenant_id=#{tenant} AND source_system='marketing-referral' AND source_request_id=#{request}</select>
<update id="update">UPDATE mk_referral_award_held_review SET check_sequence=#{next},state_name=#{status},current_revision=#{revision},cancel_revision=#{cancel},checked=#{checked},checked_nanos=#{checked_nanos},valid_until=#{valid_until},valid_until_nanos=#{valid_until_nanos} WHERE tenant_id=#{tenant} AND source_system='marketing-referral' AND source_request_id=#{request} AND binding_digest=#{digest} AND check_sequence=#{sequence}</update>
</mapper>
